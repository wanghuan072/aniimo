import { createMetadata } from "@/seo/metadata";
import { tdk } from "@/seo/tdk";
import SourcesPage from "@/page/SourcesPage";

export const metadata = createMetadata(tdk.howItWorks);

export default SourcesPage;
