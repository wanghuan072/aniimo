# Project guidance

This site is a static Astro project. Preserve existing URLs, content, data, layout, and browser interactions when changing implementation. Pages belong in `src/pages/`, shared layouts and components in `src/layouts/` and `src/components/astro/`, and client interactions use native TypeScript. Run `npm run check`, `npm run build`, and the Astro smoke test after changes that affect routes or interactions.
